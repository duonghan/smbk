/*
* Author: Duong Han
* HUST
* Design Messages
*
* This contains all the text for the AddNewSurvey component.
*/

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AddNewSurvey.Design';

export default defineMessages({
  toolboxHeader: {
    id: `${scope}.toolboxHeader`,
    defaultMessage: 'Công cụ',
  },
  propertiesHeader: {
    id: `${scope}.propertiesHeader`,
    defaultMessage: 'Thuộc tính',
  },
});
